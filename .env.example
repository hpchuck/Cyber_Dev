# Admin authentication
# Do NOT configure admin credentials using VITE_*-prefixed variables, as they are exposed to the client.
# Implement admin authentication in a trusted backend (e.g. Netlify Functions, Supabase, or another server)
# and store any sensitive credentials in private, non-VITE_ environment variables on that backend.
